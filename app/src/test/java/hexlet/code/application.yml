server:
  port: ${PORT:7070}

base-url: /api

spring:
  application:
    name: app

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml

  mvc:
    hidden-method:
      filter:
        enabled: true

  jpa:
    hibernate:
      ddl-auto: update